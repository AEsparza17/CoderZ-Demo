// En la versión demo no hay WebSocket — estas funciones son no-ops
// para mantener compatibilidad con los componentes que las importan.

export const disconnectSocket = () => {};
export const useSocket = () => {};
